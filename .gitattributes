# Normalize line endings across platforms.
* text=auto eol=lf

# Shell scripts MUST stay LF or they break on Linux/macOS (bad interpreter).
*.sh text eol=lf

# PowerShell is happiest with CRLF on Windows.
*.ps1 text eol=crlf
